# One Website (Single File)

This is the final single website code.

## Run

Just open `index.html` in browser.

or run a local server:

```bash
cd one-website
python3 -m http.server 8080
```

Open `http://localhost:8080`.

## Admin Login

- username: `me`
- password: `123`

All edits are saved in browser localStorage.
